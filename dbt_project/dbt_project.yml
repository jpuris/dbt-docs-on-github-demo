
name: "dbt_docs_demo"
version: "1.0.0"

profile: "dbt_docs_demo"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_docs_demo:
    +materialized: table
    changeme:
      +tags: ["changeme"]
      changeme:
        +tags: ["changeme"]
        +docs:
          node_color: "#71706e"
        changeme:
          +tags: ["changeme"]
          +schema: "changeme"
